[package]
name = "crate-a"
version = "0.1.0"
edition = "2021"
description = "First crate in test workspace"

[dependencies]
serde = { workspace = true }
regex = "1.0"

[dev-dependencies]
tempfile = "3.0"